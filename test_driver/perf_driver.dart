import 'package:integration_test/integration_test_driver.dart';

// Writes whatever the test reported, frame timings included, to
// build/integration_response_data.json.
Future<void> main() => integrationDriver();
